# family-tree

## Start a local webserver 

From the project root, run:

```bash
python3 -m http.server 5500
```

Then open:

```text
http://localhost:5500/
```

Use `localhost`  when testing Google Maps.

